[pytest]
testpaths = tests
markers =
    real: exercises the real government corpus; skipped when it has not been fetched
    slow: takes more than about thirty seconds
addopts = -m "not real" --strict-markers
